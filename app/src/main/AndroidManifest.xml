<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.auebds.coffeui">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoffeUI"
        tools:targetApi="31">
        <activity
            android:name=".ui.schedule.manage.ManageScheduleActivity"
            android:exported="false" />
        <activity android:name=".ui.drinks.chocolate.CreateChocolateActivity"
            android:exported="false" />
        <activity
            android:name=".ui.schedule.create.CreateScheduleActivity"
            android:exported="false" />
        <activity android:name=".ui.drinks.espresso.CreateEspressoActivity"
            android:exported="false" />
        <activity android:name=".ui.drinks.french.CreateFrenchActivity"
            android:exported="false" />
        <activity android:name=".ui.drinks.tea.CreateTeaActivity"
            android:exported="false" />
        <activity
            android:name=".MainMenuActivity"
            android:exported="true"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/Theme.CoffeUI.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

</manifest>